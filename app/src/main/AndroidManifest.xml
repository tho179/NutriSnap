<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="NutriSnap"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NutriSnap">

        <!-- Login as the Launcher Activity -->
        <activity
            android:name=".auth.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <activity
            android:name=".auth.RegisterActivity"
            android:exported="false" />

        <activity
            android:name=".auth.ForgotPasswordActivity"
            android:exported="false" />

        <activity
            android:name=".auth.VerifyOtpActivity"
            android:exported="false" />

        <activity
            android:name=".auth.ChangePasswordActivity"
            android:exported="false" />

        <activity
            android:name=".auth.NotificationSuccessActivity"
            android:exported="false" />

        <activity
            android:name=".auth.WelcomeActivity"
            android:exported="false" />

        <activity
            android:name=".ui.setup.SetupProfileActivity"
            android:exported="false" />

        <activity
            android:name=".MainActivity"
            android:exported="true" />

    </application>

</manifest>